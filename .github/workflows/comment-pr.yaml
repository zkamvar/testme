name: Comment on the pull request

# read-write repo token
# access to secrets
on:
  workflow_run:
    workflows: ["Receive Pull Request"]
    types:
      - completed

jobs:
  upload:
    runs-on: ubuntu-latest
    if: >
      ${{ github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: 'Download artifact'
        uses: actions/github-script@v3.1.0
        with:
          script: |
            var artifacts = await github.actions.listWorkflowRunArtifacts({
               owner: context.repo.owner,
               repo: context.repo.repo,
               run_id: ${{github.event.workflow_run.id }},
            });
            var matchArtifact = artifacts.data.artifacts.filter((artifact) => {
              return artifact.name == "diff" || artifact.name == "built"
            });
            var diff_download = await github.actions.downloadArtifact({
               owner: context.repo.owner,
               repo: context.repo.repo,
               artifact_id: matchArtifact[1].id,
               archive_format: 'zip',
            });
            var built_download = await github.actions.downloadArtifact({
               owner: context.repo.owner,
               repo: context.repo.repo,
               artifact_id: matchArtifact[0].id,
               archive_format: 'tar',
            });
            var fs = require('fs');
            fs.writeFileSync('${{github.workspace}}/diff.zip', Buffer.from(diff_download.data));
            fs.writeFileSync('${{github.workspace}}/built.tar.gz', Buffer.from(built_download.data));
      - run: unzip diff.zip
      - name: 'Checkout md outputs'
        uses: actions/checkout@v2.3.4
        with:
          ref: md-outputs
          path: built
          fetch-depth: 1
      - name: "Commit and push"
        run:
          ls -d -A built/* | grep -v '.git' | xargs rm -r
          tar --strip-comments=1 -C built -xzf built.tar.gz
          cd built
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git checkout -b md-outputs-PR-$(cat NR)
          git add -A
          git commit --allow-empty -m "differences for PR $(cat NR)"
          git push -u --force --set-upstream origin md-outputs-PR-$(cat NR)

      - name: 'Comment on PR'
        uses: actions/github-script@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            var fs = require('fs');
            var issue_number = Number(fs.readFileSync('./NR'));
            var body = String(fs.readFileSync('./diff.md', {encoding:'utf8', flag:'r'}));
            var id = await github.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue_number,
              body: body 
            });
            console.log(id)
